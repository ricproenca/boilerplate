import { makeStyles } from '@mui/styles';

const loginStyles = makeStyles(_theme => ({
  Container: {
    position: 'fixed',
    bottom: 0,
    right: 0,
    fontSize: 10,
    backgroundColor: 'black',
    color: 'white'
  }
}));

export default loginStyles;
